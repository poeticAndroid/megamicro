{
  "name": "megamicro",
  "version": "0.3.0",
  "description": "An attempt to create my own fantasy cpu",
  "main": "server.js",
  "scripts": {
    "build": "foreach -g ./*.wat -x \"wat2wasm #{path}\"",
    "watch": "sane \"npm run build\" . --glob=*.wat"
  },
  "keywords": [
    "fantasy-console",
    "wasm",
    "webassembly"
  ],
  "author": "poeticAndroid",
  "license": "MIT",
  "dependencies": {
    "foreach-cli": "^1.8.1",
    "sane": "^5.0.1",
    "wabt": "^1.0.25"
  }
}